mixin card(card)
    div(id=card.id class="card" data-id=card.id data-suit=card.suit data-value=card.value data-color=card.color)
        img(src=`https://hifreecell-mini.s3.amazonaws.com/images/${card.id}.png` crossorigin)